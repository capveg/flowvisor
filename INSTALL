REQUIREMENTS:
    - ant           (no version requirements?)
    - java 1.6

BUILD:
    - run `make` (or `ant` : Makefile is just a wrapper)

CONFIGURE:
    - Edit scripts/envs.sh to install dir (FIXME!)
    - Create SSL and FlowVisor configs:
        use ./scripts/config-gen-default.sh
        - creates default-config.xml
RUN: 
    To run flowvisor:
        ./scripts/flowvisor.sh config.xml

    To configure running flowvisor, use ./scripts/fvctl.sh
        FIXME: document fvctl
